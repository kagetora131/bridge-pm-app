# CLAUDE.md（ブリッジPM：海外オフショア連携向けプロジェクト管理アプリ）

> Claude Code に向けたプロジェクト概要・実装指示書です。

## 作りたいもの

海外オフショア開発チームと日本側の橋渡し役（ブリッジSE／PM）向けの、**多言語対応・タイムゾーン計算機能つきのプロジェクト管理アプリ**。

- タスク管理（多言語UI切り替え：日本語⇔英語⇔ベトナム語など）
- タスクの説明文をAIが自動翻訳し、両言語で同時表示
- メンバーごとの現地時刻を表示し、**全員が参加しやすい会議時間帯をAI/ロジックで自動提案**
- 開発でよく使う技術用語の対訳集（用語集）をチームで蓄積・検索できる

## 解決したい課題

- 誰が：オフショア開発プロジェクトのブリッジSE・PM、および日本側/海外側のメンバー
- どんな場面で：日本企業と海外の開発チームが共同でプロジェクトを進める場面
- 何に困っているか：
  - 言語の壁により、タスクの意図や仕様の細かいニュアンスが正確に伝わらず、手戻りが発生する
  - タイムゾーンの違いにより、会議設定や「今、相手は稼働時間内か」の把握に手間がかかる
  - 技術用語の訳語がメンバーごとにバラバラになり、認識のズレを生む
  - JiraやAsanaなど既存の汎用PMツールは多言語UIはあっても、上記のような「オフショア特有の橋渡し業務」に特化した機能までは持っていない

## 使いたい技術（無料・ライセンスクリア前提）

- フロントエンド：React（MITライセンス、無料）
- 翻訳・要約：Claude API（タスク説明文の翻訳、進捗レポートの多言語生成などに利用）
- タイムゾーン計算：`date-fns-tz` または標準の `Intl` API（いずれも無料。date-fns-tzはMITライセンス）
- バックエンド／データ：Supabase（オープンソース、Postgresベースの無料枠あり。認証・タスクデータの永続化に利用）
- ホスティング：Vercel 無料枠 または GitHub Pages

> 注意：Claude APIは従量課金制です。個人開発・ポートフォリオ用途なら少額で収まりますが、無制限に無料ではない点は留意してください。

## 入力

- タスク情報（タイトル、説明、担当者、期限、ステータス）
- メンバー情報（氏名、所属拠点、タイムゾーン、使用言語）
- 会議調整をしたいメンバーの組み合わせ

## 出力

- 多言語で表示されるタスク一覧（原文＋AI翻訳を並べて表示、または言語切り替えで表示）
- メンバーごとの現地時刻をリアルタイム表示
- **指定したメンバー全員の稼働時間帯が重なる「会議に適した時間帯」の自動提案**（差別化のコア機能）
- 技術用語の対訳集（検索・登録機能）
- （将来的に）進捗レポートの多言語自動生成

## 差別化のコア機能：会議時間帯の自動提案ロジック

1. 各メンバーの「稼働時間帯」（例：日本 9:00-18:00 JST、ベトナム 8:00-17:00 ICTなど）を設定
2. 参加させたいメンバーを選択すると、全員の稼働時間帯をタイムゾーン変換したうえで重なりを計算
3. 重なりが少ない/ない場合は「もっとも負担が少ない妥協案」（例：日本側が早出、海外側が残業など）も提示できるようにする
4. UIとしては、各メンバーの1日のタイムライン（横棒）を重ねて表示し、視覚的に重なりがわかるようにするとわかりやすい

## 実装の進め方（MVP方針）

1. タスクの基本CRUD（作成・一覧・ステータス変更）を実装
2. メンバーのタイムゾーン設定機能と、現地時刻のリアルタイム表示を実装
3. 会議時間帯の自動提案ロジックを実装（差別化の核なので優先度高め）
4. Claude APIによるタスク説明文の自動翻訳を追加
5. （後回しでOK）用語集機能、進捗レポートの多言語自動生成

## 未確定事項（要確認しながら進める）

- 対応言語の範囲（まず日本語・英語のみにするか、ベトナム語なども最初から含めるか）
- 用語集をチーム単位で共有する場合、認証・権限管理をどこまで作り込むか
- 会議時間帯の提案ロジックの詳細（単純な重なり計算にするか、過去の会議実績なども加味するか）
